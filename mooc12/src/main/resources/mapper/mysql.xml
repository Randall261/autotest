<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.course.client.model">
    <select id="loginCase" parameterType="Integer" resultType="com.course.client.model.LoginCase">
        SELECT
            *
        FROM
            loginCase
        WHERE
            id = #{id};
    </select>

    <select id="addUserCase" parameterType="Integer" resultType="com.course.client.model.AddUserCase">
        SELECT
            *
        FROM
            addUserCase
        WHERE
            id = #{id};
    </select>

    <select id="getUserInfoCase" parameterType="Integer" resultType="com.course.client.model.GetUserInfoCase">
        SELECT
            *
        FROM
            getUserInfoCase
        WHERE
            id = #{id};
    </select>

    <select id="getUserListCase" parameterType="Integer" resultType="com.course.client.model.GetUserListCase">
        SELECT
            *
        FROM
            getUserListCase
        WHERE
            id = #{id};
    </select>

    <select id="updateUserInfoCase" parameterType="Integer" resultType="com.course.client.model.UpdateUserInfoCase">
        SELECT
            *
        FROM
            updateUserInfoCase
        WHERE
            id = #{id};
    </select>
</mapper>